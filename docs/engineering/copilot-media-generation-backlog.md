# Copilot media generation (backlog)

Focused **media-generation** sessions (copilot workbench, composer “Focused copilot” handoff, inline **Generate**) are **disabled by default**.

**Enable locally:** `NEXT_PUBLIC_COPILOT_MEDIA_GENERATION_ENABLED=true`

**Follow-up:** define in-chat UX—render targets in the thread, session lifecycle, and editing/generation flow—then re-enable by default or remove the flag.

See `src/constants/copilotFeatures.ts`.
