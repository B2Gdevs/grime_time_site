'use client'

import React from 'react'
import { DatabaseZapIcon, LayoutDashboardIcon, ShieldCheckIcon } from 'lucide-react'

import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@/components/ui/card'
import { cn } from '@/utilities/ui'

export function AdminLoginPanel({ className }: { className?: string }) {
  return (
    <div className={cn('flex flex-col gap-4', className)}>
      {/* <>do not put anything here</> */}
    </div>
  )
}
