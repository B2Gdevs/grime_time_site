/**
 * Default JWT strategy name used by Payload when issuing session tokens.
 * Keep in sync with `JWTAuthentication` in `payload` (default `strategyName`).
 */
export const PAYLOAD_LOCAL_JWT_STRATEGY = 'local-jwt' as const
